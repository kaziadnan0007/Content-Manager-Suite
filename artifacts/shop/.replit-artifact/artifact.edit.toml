kind = "web"
previewPath = "/"
title = "ShopBD"
version = "1.0.0"
id = "artifacts/shop"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/shop run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/shop", "run", "build" ]
publicDir = "artifacts/shop/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
